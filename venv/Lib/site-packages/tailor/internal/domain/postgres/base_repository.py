# -*- coding: utf-8 -*-
from sqlalchemy.orm import Session

from tailor.internal.domain.postgres import SessionFactory


class BaseRepository:

    def __init__(self):
        self._session = self.__create_postgres_database_client()

    def __create_postgres_database_client(self) -> Session:
        factory = SessionFactory.Instance()

        return factory.create()
